[project]
name = "eddn-tail"
version = "0.1.0"
description = "A KinesisTail-like TUI for monitoring the EDDN live stream"
requires-python = ">=3.9"
dependencies = [
    "pyzmq>=25.0",
    "textual>=0.40",
    "rich>=13.0",
]

[project.scripts]
eddn-tail = "eddn_tail:main"
